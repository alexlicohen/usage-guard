name: check
on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: shellcheck
        run: shellcheck usage_guard.sh test/run.sh
      - name: tests
        run: bash test/run.sh
